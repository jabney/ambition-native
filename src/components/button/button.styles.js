import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    borderRadius: 5,
    overflow: 'hidden',
  },
  text: {
    color: 'white',
    backgroundColor: '#9bf',
    padding: 6,
    textAlign: 'center',
  },
})

export default styles
