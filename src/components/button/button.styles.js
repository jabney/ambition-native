import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    borderRadius: 5,
    overflow: 'hidden',
  },
  button: {
    backgroundColor: '#9bf',
    padding: 6,
  },
  text: {
    color: 'white',
    textAlign: 'center',
  },
  disabled: {
    backgroundColor: '#aaa',
  },
})

export default styles
