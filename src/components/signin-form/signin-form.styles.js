import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {},
  input: {
    backgroundColor: '#fff',
    height: 28,
    borderWidth: 1,
    borderColor: 'gray',
    marginBottom: 6,
  },
  button: {
    marginTop: 12,
  },
  link: {
    marginTop: 12,
  },
})

export default styles
